-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 7920
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 15839
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 23758
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 31677
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 39596
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 47515
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 55434
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 63353
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 71272
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 79191
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 87110
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 95029
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 102948
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 110867
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 118786
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 126705
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 134624
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 142543
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 150462
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 158381
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 166300
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 174219
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 182138
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 190057
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 197976
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 5895
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 13814
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 21733
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 29652
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 37571
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 45490
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 53409
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 61328
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 69247
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 77166
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 85085
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 93004
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 100923
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 108842
  and measured_at >= date '2021-07-28'
order by measured_at;
-- Consulta 2 (ancho): la misma serie, leyendo una columna de la tabla de la prueba.
select measured_at, hidrogeno_h2_ppm
from bench.w_analisis_cromatografico
where equipment_id = 116761
  and measured_at >= date '2021-07-28'
order by measured_at;
